import React from 'react';
import Typography from '@material-ui/core/Typography';

const NoIdeas = ({ text }) => <Typography align="center" variant="title">{text}</Typography>

export default NoIdeas;